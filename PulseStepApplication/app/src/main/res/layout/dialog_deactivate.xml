<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/dialog_background">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:fontFamily="@font/overpass_bold"
        android:text="Deactivate Account"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/dialog_message"/>

    <TextView
        android:id="@+id/dialog_message"
        android:lineSpacingExtra="3dp"
        android:text="Are you sure you want to deactivate your account? All data associated with this account will be permanently lost and cannot be recovered."
        android:fontFamily="@font/overpass_bold"
        android:textColor="@color/gray"
        android:textSize="12sp"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/title"/>

    <LinearLayout
        android:orientation="horizontal"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/dialog_message">

        <Button
            android:id="@+id/positive_button"
            android:text="Confirm"
            android:textColor="@color/black"
            android:background="@drawable/bt_yes"
            android:layout_weight="1"
            android:layout_width="10dp"
            android:layout_marginEnd="10dp"
            android:fontFamily="@font/overpass_bold"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/negative_button"
            android:text="Cancel"
            android:textColor="@color/black"
            android:background="@drawable/bt_no"
            android:layout_weight="1"
            android:layout_width="10dp"
            android:layout_marginStart="10dp"
            android:fontFamily="@font/overpass_bold"
            android:layout_height="wrap_content"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>