<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/lighter_orange"
    tools:context=".LikeActivity">

    <!-- Toolbar containing the back button and title -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_notifications"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/lighter_orange"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:contentDescription="Back"
            android:paddingStart="5dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:src="@drawable/ic_back_arrow" />

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/overpass_bold"
            android:paddingLeft="10dp"
            android:paddingTop="8dp"
            android:text="Notifications"
            android:textColor="@color/black"
            android:textSize="20sp" />

    </androidx.appcompat.widget.Toolbar>


    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/bt_switch_rank"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar_notifications"
        android:gravity="center"
        android:paddingTop="30dp"
        app:checkedButton="@id/bt_daily"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="-15dp"
        android:paddingLeft="15dp"
        app:selectionRequired="true"
        app:singleSelection="true">

        <Button
            android:id="@+id/bt_daily"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/bt_daily"
            android:textColor="@drawable/bt_text_color_selector2"
            android:backgroundTint="@drawable/bt_background_color_selector"
            android:fontFamily="@font/inter_bold"
            app:strokeColor="@color/white" />

        <Button
            android:id="@+id/bt_monthly"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/bt_monthly"
            android:textColor="@drawable/bt_text_color_selector2"
            android:backgroundTint="@drawable/bt_background_color_selector"
            android:fontFamily="@font/inter_bold"
            app:strokeColor="@color/white" />

    </com.google.android.material.button.MaterialButtonToggleGroup>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/likeList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/bt_switch_rank"
        android:layout_marginTop="20dp" />
</RelativeLayout>