<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PulseStepApplication" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="materialButtonStyle">@style/bt_workout</item>

    </style>

    <style name="Theme.PulseStepApplication" parent="Base.Theme.PulseStepApplication" />
    <style name="Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/lighter_orange</item>
    </style>
    <!-- 自定义 Bottom NavigationView 样式 -->

    <!-- 自定义按钮样式 -->
    <style name="bt_workout" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@null</item> <!-- 防止背景色被主题覆盖 -->
        <item name="background">@drawable/bg_ripple</item> <!-- 防止背景色被主题覆盖 -->
    </style>
</resources>